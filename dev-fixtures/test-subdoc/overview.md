---
id: overview
title: Overview
---

This subdoc is a **dev-only fixture** — it only appears when running `pnpm website:dev` and is never included in production builds.

Use it to test subdoc-related features: sidebar rendering, navbar dropdown entries, routing, redirect behaviour, etc.
